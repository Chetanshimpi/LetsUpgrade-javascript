//program for convert minutes to second

function ConMinToSec(minutes)
{
    return Math.floor(minutes * 60);
}
var MinToSec=ConMinToSec(2); //conver min to sec in js
document.write("result of con min to sec :- " + MinToSec);
